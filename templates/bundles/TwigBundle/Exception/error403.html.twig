{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto px-8">
        <div class="flex justify-center">
            <img src="{{ asset('images/loup.svg') }}" alt="Loup" class="w-64">
        </div>
        <div class="text-center">
            <h2 class="text-3xl font-bold">Il semblerait que tu ne sois pas assez sage pour faire cela !</h2>
            <p class="text-xl">Laisse moi t'apprendre à le devenir.</p>
            <div class="mt-4">
                <a href="{{ path('app_index') }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 focus:outline-none">Retour à l'accueil</a>
            </div>
            <div class="mt-3 text-xs">
                Erreur {{ status_code }} - {{ status_text }}
            </div>
        </div>
    </div>
{% endblock %}