<div{{ attributes }} class="my-4 container mx-auto px-8">
    <hr class="container mx-auto h-px my-8 w-32 bg-gray-400 border-0">
    <div class="flex items-center justify-center">
        <h2 class="text-2xl">{{ changelog.name }}</h2>
        <div class="h-4 w-4"></div>
        <div class="text-gray-500 text-sm">{{ changelog.releasedAt|date("d/m/Y") }}</div>
    </div>
    <div class="mt-2">
        {{ changelog.content }}
    </div>
</div>
